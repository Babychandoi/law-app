# Features

Feature modules should live here as the app is migrated away from the legacy `page` and `component` folders.

Keep new feature code grouped by business area, for example `home`, `news`, `services`, `admin`, and `chat`.
